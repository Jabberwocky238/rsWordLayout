# cjk-plain 采集，2026-09-18（Mac Word）

**本仓库第一份含 CJK 字形的采集。** 此前 22 份采集包、17231 个字形里一个都没有。

判据：`docs/PREREG-2026-09-18-cjk-plain.md`。夹具 sha256 `07dea76d27bb…`，
字体 **Songti SC**（`fixtures/plain.docx` 指定的 SimSun 本机 Word 列不出，
采下去就是字体替换，§6.2）。

判定 **EVALUATED**：

| | |
| --- | --- |
| Z1 基线在 0.24pt 栅格上 | **301/301 成立** |
| Z2 行距 = N7（自然行高 × 1.3） | **4/4 成立**，四个字号逐个对上 |
| Z3 段落标记画 1 个空格 | **27/27 成立** |
| Z4 行首 x = 72.0000pt | **33/33 成立** |
| Z5 断行逐行相同 | **1/2**：无标点那段逐行相同，含标点那段引擎多断一行 |
| Z6 行首禁则 | **判不了**：本夹具上禁则根本没被触发过 |

**Z5 的原因量到了**：Word 让行末的 `。` 挂到右边界**外面**
（起点 523.33pt > 边界 523.20pt，行末到 538.30pt）——标点溢出边界。
引擎没有这条，把末 3 个字符推成了新的一行。

**F1–F4 是本仓库第一次真正扫。** 初扫命中 F3 六十八处，全部是子集字体把汉字
解成康熙部首（`文 U+6587` → `⽂ U+2F42`）；按 Unicode 的兼容分解改掉等价规则后归零。
