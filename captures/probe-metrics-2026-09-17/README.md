# probe-metrics 采集，2026-09-17（Mac Word）

判据：`docs/PREREG-2026-09-17-probe-metrics.md`（**采集前**提交，见 `15bd20a`）。
夹具：`fixtures/probe-metrics.docx`，sha256 `faffc295…`。

**判定：VOID。** 证否条件 F-E 在 8 个探针上触发，按预注册 §4，本批读数全部作废。
不挑着用，不改判据再判一遍——那正是预注册要挡的事。

`case.pdf` 与 `case.docx` 没收进来：夹具本身在 `fixtures/` 下可复现，
PDF 由它导出。读数全在 `glyphs.json` 与 `sweep.json` 里。

采集条件：Word for Mac，文档以**兼容性模式**打开。§6.6：Mac 与 Windows 是两套
字体度量，本批的任何观察都只能写「在 Mac Word 兼容性模式下观测到」。
