# vmisc3 第一次采集：**VOID**（2026-09-17）

判据：`docs/PREREG-2026-09-17-vmisc3.md`。夹具 sha256 `8b627709…`。

判定 **VOID**，触发 **F-B**，但**账外字体是空列表**——
触发的是另一半：**申请的 `Times New Roman` 没有在 PDF 里出现**。

本批夹具里每个段落都有可见 run，没有 vmisc2 那种「只含 `w:br` 的空段落」，
所以 Word 根本用不到默认字体。我沿用上批的申请列表时没想到这一点。

这暴露了核查逻辑的一个真缺陷：**「申请了却没出现」与「出现了不该出现的」是两回事**，
前者对可选的回退字体根本不适用。已修（加 `optional_families`）。

作废照判。判据脚本在算 R0–R3 之前就短路，`predictions` 是空的。
