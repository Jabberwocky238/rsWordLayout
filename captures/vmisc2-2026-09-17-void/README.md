# vmisc2 第一次采集：**VOID**（2026-09-17）

判据：`docs/PREREG-2026-09-17-vmisc2.md`。夹具 sha256 `1b0be91f…`。

判定 **VOID**，触发 **F-B**：PDF 里出现 `TimesNewRomanPSMT`，一个没申请的字体。

**这是真的字体替换。** 查清楚了：Times 只出现在**空格**字形上，
而且只在**只含 `w:br` 的那个段落**的段落标记处——那种段落没有任何可见 run，
Word 就用**默认字体**画它的段落标记，`w:pPr/w:rPr` 里指定的字体没管住。

这是 Word 的行为，不是夹具字节坏了；但既然那个字形不在申请之列，F-B 触发就是对的。
作废照判，**没有例外**。

作废时判据脚本在算 Q1/Q2/Q3 之前就短路，`predictions` 是空的——
关于本批预测的数字一个都没产生过。
