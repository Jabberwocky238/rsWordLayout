# 预注册：page-start 夹具（2026-09-17）

**本文在采集之前写死并提交。** 采集后只允许追加「结论」一节（量具方法 §7.1）。

夹具：`fixtures/page-start.docx`，sha256
`e9c933739fd462eb8b440438fe93a1323d6220f6c1e8c5caf8b36a4c2905dcb1`
（第一版 `78550ef1…` 采集后作废，见 §7.1）
判据的可执行形式：`tools/measure/prereg_pagestart.py`（与本文同一次提交）。

## 0 为什么要这一份

首行是唯一一项从头到尾没对上过的。试过的形式与它们的成绩（三批合计 60 个首行读数）：

| 形式 | 合计 |
| --- | ---: |
| `72 + quantize(natural) − quantize(descent)`（= T1，**已判假**） | 57 / 60 |
| `quantize(72 + hhea.ascender + lineGap)`（**已判假**） | 45 / 60 |
| `quantize(72 + usWinAscent/upem × 字号)`（**已判假**，12 组分开里 0 中） | — |

而**量化次序已证恒等、穷尽**（见 baseline-order 判据文 §0）。

本批换一个层面：**前五批每一组都用 `w:pageBreakBefore` 起页**，所以那 60 个首行
读数**全部**来自「强制分页之后的第一行」。这一维从头到尾只有一个实例——
正是 §7.2 警告的情形：检验实例与设计实例在被判的那一层上没有区别。

若 Word 对「强制分页起的首行」与「自然溢出起的首行」处理不同，那前五批所有首行
读数都带着同一个混杂因素，而首行恰恰是唯一一直不对的那一项。

**这个怀疑不来自残差**，来自「我自己的夹具在这一维上没有变化」。所以本批线索
**独立于已见读数**——与 baseline-order 那批不同，那批得标注线索不纯净，这批不必。

## 1 容差

**0.0000pt**，逐位相等。唯一例外：PDF 读数的十进制舍入取 **1e-6 pt**。

## 2 夹具怎么造出两种首行

每组连排 N 段同设置的短段落，N 取「排满一页再多 3 行」：

- 第 1 段带 `w:pageBreakBefore` → 本组**第 1 页**的首行是**强制分页**起的；
- 内容溢出 → 本组**第 2 页**的首行是**自然溢出**起的。

同组内字体、字号、行距设置**完全相同**，所以两者若不等，只能是起页方式造成的。

## 3 预测

### P0 — 每条基线仍落在栅格上

**判为假**：任何一条基线不是 0.24pt 的整数倍。分母 = 采集到的全部基线。

六个族（Brush Script MT / Khmer Sangam MN / Silom / Lao Sangam MN /
AppleMyungjo / Arial Unicode MS）与前五批无一重合，`upem` 含 **1025**
（AppleMyungjo），`lineGap` 含 **380**（Khmer Sangam MN）。这是第**六**次独立检验。

### P1 — 起页方式不影响首行基线（**本批的点预测**）

```
预测：同一组内，第 2 页的首行基线 − 第 1 页的首行基线 = 0.0000pt
```

**判为假的读数**：任何一组两者不等。分母 = **18**（18 组）。

**这一条不依赖任何公式**，因此也不受「首行到底该用哪个式子」未决的影响。
它只问一件事：起页方式算不算一个变量。

- **成立** → 起页方式不是混杂因素，前五批的首行读数可以合并看，
  问题在别处；
- **判假** → 找到了一个真实的混杂因素，且前五批 60 个首行读数**全部**只覆盖了
  其中一种，那些否定结论的范围要相应收窄。

## 4 对照（**不作预测**）

把 T1（目前最好、已判假的形式）在两类首行上分别算出来报出，好看出
「不对的那些」是不是集中在某一类。分高不改判——T1 已在 first-line 判假。

## 5 采集前声明的排除项

某组**没有跨到第 2 页**（或跨了 3 页以上）时，该组**判不了**，记入分母但不算通过。
夹具已按各字体各字号算过每页能排几行并多留 3 行，正常排版下不该触发。

## 6 证否条件（量具自身）

| | |
| --- | --- |
| **F-A** | 采前核查不是 `PASS` |
| **F-B** | 采集包里的字体核查不是 `PASS`（判据直接采信采集包的结论） |
| **F-C** | 夹具 sha256 与本文声明的不一致 |
| **F-D** | 行号扫描给出的页数与 PDF 页数不符 |

依旧**没有**「两个读数不等即作废」那一条。

## 7 采集条件

- **Mac**，Word for Mac，**兼容性模式**。
- 字体族用 Word 自己列出来的名字：`Brush Script MT` / `Khmer Sangam MN` /
  `Silom` / `Lao Sangam MN` / `AppleMyungjo` / `Arial Unicode MS`。

## 7.1 夹具的修订（**第一次采集之后**）

第一次采集判 **VOID**，触发 F-B：PDF 里出现 **Cambria**，一个没申请的字体。

**这次是真的字体替换。** Kokonor 与 Gurmukhi MN 没有拉丁字形（读 `cmap`：
Kokonor 缺全部数字与字母，Gurmukhi MN 缺全部字母），标签被 Word 用 Cambria
代画。那几组根本不是用申请的字体排的。

作废是**对的**，不是误报。随后两处改动：

1. 夹具换掉那两个族（改用 `AppleMyungjo` upem 1025、`Arial Unicode MS`），
   sha256 随之更新；
2. 把这道关**前移到生成夹具时**：`wordmeasure/fontcover.py` 的 `assert_can_draw`
   读 `cmap` 核对标签字符，画不出就直接报错。画不出标签是生成时就能查的事，
   不该等采完才发现整批作废。

**P0 / P1、容差、分母、排除项、其余证否条件一个字未动。** 作废时判据脚本在算
P0/P1 之前就短路，`VERDICT.json` 的 `predictions` 是空的——关于本批预测的数字，
一个都没产生过。可核：`captures/page-start-2026-09-17-void/`。

## 8 结论

采集包：`captures/page-start-2026-09-17/`。Word for Mac，36 页，1288 字形，
前置核查 PASS，字体核查 PASS，四条证否条件一条都没触发。

| | 判据 | 结果 | |
| --- | --- | ---: | --- |
| **P0** | 每条基线落在 0.24pt 栅格上 | **322 / 322** | **成立** |
| **P1** | 起页方式不影响首行基线 | **18 / 18** | **成立** |
| 对照 | T1 在两类首行上的命中 | 各 **8 / 18** | *不作预测* |

### P0 成立：栅格第六次独立检验

累计：**六份互相独立的夹具、1546 条基线、零例外**。

### P1 成立：起页方式不是变量

**18 / 18。** 同一组内，强制分页起的首行与自然溢出起的首行，基线**逐位相等**。

这一条不依赖任何公式，所以它的成立不受「首行该用哪个式子」未决的影响。
**而且本批线索独立于已见读数**（判据文 §0），所以这是一次干净的肯定。

两条推论：

1. **前五批那 60 个首行读数可以合并看**——它们虽然全部来自强制分页，
   但起页方式既然不是变量，那个范围限制就不存在了。此前几批的否定结论
   （`usWinAscent` 被排除、量化次序穷尽）**范围不必收窄**。
2. **首行的问题不在起页方式。** 得往别处找。

对照也指着同一个方向：T1 在两类首行上**各 8 / 18，一模一样**。
它错得很均匀，不偏向任何一类。

### T1 的成绩更新

| 批次 | T1 |
| --- | ---: |
| accumulation | 11 / 12 |
| first-line | 22 / 24 |
| baseline-order | 24 / 24 |
| **page-start** | **16 / 36**（两类各 8/18） |
| 合计 | **73 / 96** |

本批的族比前几批极端得多（`Khmer Sangam MN` 的 `lineGap` 380、
`Arial Unicode MS` 的 descent 555、`AppleMyungjo` 的 upem 1025），
T1 在这里露馅远多于前三批。**它确实不对，而且新字体让这一点更清楚。**

### 对引擎的意义

**引擎这一轮仍不动，但这次是「确认无需动」而不是「无据可动」：**

P1 成立说明引擎**不该**为两种起页方式分别处理——它现在也没有，一致。
若哪天有人想给「分页后的第一行」加特例，这一条 18/18 就是拦住它的依据。

首行形式仍未确立，T1 在本批 16/36，比前三批差得多，所以更不能把它写进引擎。
